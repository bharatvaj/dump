<Page
    x:Class="File360.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:File360"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Frame x:Name="ScenarioFrame" Grid.Row="0" Navigated="ScenarioFrame_Navigated"/>
        <Border x:Name="StatusBorder" Grid.Row="1" Visibility="Collapsed">
            <TextBlock x:Name="StatusBlock" Foreground="#FFD10505" FontWeight="Bold" TextWrapping="Wrap" FontSize="20"/>
        </Border>
        <local:DialogBox IsOpen="False" HeadingText="  ARE YOU SURE?" ContentText="  ...are you sure you want to exit?" x:Name="DialogBox"/>
    </Grid>
</Page>
